CREATE TABLE IF NOT EXISTS users (id serial PRIMARY KEY, email VARCHAR NOT NULL UNIQUE, password VARCHAR NOT NULL, role integer NOT NULL, age INTEGER NULL, sex VARCHAR NULL, name VARCHAR NULL, last_name VARCHAR NULL, login VARCHAR(255) NOT NULL UNIQUE, CONSTRAINT role_user FOREIGN KEY(role) REFERENCES roles(id));